<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pix.testandroidproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".testbinding.TestBindingActivity" />
        <activity
            android:name=".testframework.TestFrameworkActivity"
            android:label="@string/title_activity_test_framework"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".testframework.MVCActivity"></activity>
    </application>

</manifest>